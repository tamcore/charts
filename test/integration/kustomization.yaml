apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: matrix-ci

resources:
  - namespace.yaml
  - postgres.yaml
  - synapse-config.yaml
  - synapse-registrations.yaml
  - synapse.yaml
