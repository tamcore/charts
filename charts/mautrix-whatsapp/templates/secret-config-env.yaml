{{- include "mautrix-whatsapp.configEnv.validate" . -}}
{{- if and .Values.configEnv.enabled (not .Values.configEnv.existingSecret) .Values.configEnv.secretEnv -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "mautrix-whatsapp.fullname" . }}-config-env
  labels:
    {{- include "mautrix-whatsapp.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $k, $v := .Values.configEnv.secretEnv }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
{{- end -}}
