apiVersion: v1
kind: Service
metadata:
  name: {{ include "eufy-security-ws.fullname" . }}
  labels:
    {{- include "eufy-security-ws.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eufy-security-ws.selectorLabels" . | nindent 4 }}
